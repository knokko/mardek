package mardek.assets.area.objects

class AreaSwitchOrb(
	val x: Int,
	val y: Int,
	val color: String,
	val convAction: Boolean, // false for switches in sun temple
	val gold: Boolean, // false in sun temple 4
) {
}
