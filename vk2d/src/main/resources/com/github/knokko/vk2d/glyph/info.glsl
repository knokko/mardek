struct GlyphInfo {
	vec2 subpixelOffset;
	uint horizontalInfoOffset;
	uint verticalInfoOffset;
	vec2 renderRegionSize;
	uvec2 intRegionSize;
	vec4 fillColor;
	vec4 strokeColor;
	vec4 backgroundColor;
	float strokeWidth;
};
